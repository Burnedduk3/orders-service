# configs/config.yaml
environment: development

version: 1.0.0

log_level: info

server:
  port: "8200"
  host: "0.0.0.0"
  read_timeout: "30s"
  write_timeout: "30s"
  cors:
    allow_origins: ["*"]

database:
  host: "localhost"
  port: "5432"
  username: "orders-service"
  password: "admin"
  database: "orders-service"
  ssl_mode: "disable"


security:
  rate_limit_rps: 100
  rate_limit_burst: 200

logging:
  level: "debug"
  format: "text"